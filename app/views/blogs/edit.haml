- content_for :head do
  = javascript_include_tag 'admin'
.padding
  #new_blog
    %h1 Editing blogs
    = render :partial => 'form', :locals => { :submit_button => 'Update' }
    = link_to 'Show', @blog
    %span
      |</span#{link_to 'Back', blogs_path}
